[package]
name = "torus-bridge-program"
version = "0.1.0"
edition = "2021"
description = "SP1 guest program for proving torus-core PoS block validity"

[dependencies]
torus-kernel = { path = "../torus-kernel" }
# Uncomment when SP1 SDK is installed:
# sp1-zkvm = "4"
